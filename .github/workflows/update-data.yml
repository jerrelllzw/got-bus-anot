name: Update bus data

# Refresh the static per-service data shards from LTA DataMall.
# Runs daily so schedule tweaks and the public-holiday list are picked up
# promptly; a run with no data changes commits nothing. workflow_dispatch lets
# you trigger a manual run (e.g. the first time, to generate real data).
on:
  schedule:
    # 09:00 UTC daily = 17:00 Singapore time.
    - cron: "0 9 * * *"
  workflow_dispatch:

# Allow the job to push the refreshed data back to the repo.
permissions:
  contents: write

# Never run two refreshes at once.
concurrency:
  group: update-data
  cancel-in-progress: false

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Fetch data from LTA DataMall
        env:
          # Add this in: Settings -> Secrets and variables -> Actions.
          LTA_ACCOUNT_KEY: ${{ secrets.LTA_ACCOUNT_KEY }}
        run: python scripts/fetch_data.py

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # Stage shards, the index, any removed services, and the
          # auto-refreshed public-holiday list.
          git add -A data/svc data/services.json data/holidays.json
          # Only commit if the data actually changed.
          if git diff --staged --quiet; then
            echo "No changes to bus data — nothing to commit."
          else
            git commit -m "chore: refresh bus data $(date -u +%Y-%m-%d)"
            git push
          fi
